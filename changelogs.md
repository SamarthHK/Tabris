<h1>ChangeLogs</h1>
<li>
<ol>Created InjectionFiles.java template for InjectionFiles.json config</ol>
<ol>
<li>
<ol>Created InjectionLocator.java that scans through InjectionFiles.json</ol>
<ol>gets all the files that needs injection</ol>
<ol>Scans through the files for all template markers, look through InjectionType Enum for that</ol>
</li>
</ol>
<ol>Added a Enum InjectionType.java for types of injections i support</ol>
<ol>Added InjectionLocation record just to store start,end,InjectionType</ol>
</li>

